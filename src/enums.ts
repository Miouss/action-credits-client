export enum ActionName {
  INVITE = "Invite",
  SEND_MESSAGE = "Send Message",
  VISIT = "Visit",
}

export enum User {
  WAALAXY = "Waalaxy",
  SAMIR = "Samir",
  FABIEN = "Fabien",
}

export enum API_ENDPOINTS {
  AUTH = "/auth",
  ACTIONS = "/actions",
  ACTIONS_QUEUE = "/actions/queue",
}
