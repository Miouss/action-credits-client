export enum ActionName {
  INVITE = "Invite",
  SEND_MESSAGE = "Send Message",
  VISIT = "Visit",
}

export enum API_ENDPOINTS {
  ACTIONS = "/actions",
  CONFIG = "/config",
  QUEUE = "/queue",
}

export enum ActionStatus{
  PENDING = "pending",
  EXECUTED = "executed"
}
